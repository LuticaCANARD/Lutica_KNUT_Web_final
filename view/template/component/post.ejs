<div>
    <div class="card mb-3" style="max-width: 540px;">
        <div class="row g-0">
            <% if(postPic[String(post.id)]){ %>
            <div class="col-md-4">
                <% for(let j=0; j<postPic[String(post.id)].length ;j++) { %>
            <%- include('./image.ejs',
            {
                src:   postPic[String(post.id)][j].path  ,
                classes:"post-photo",etc:false 
            })%>
            <% } %>
            </div>
            <% } %>
          <div class="col-md-8">
            <div class="card-body">
              <h5 class="card-title"><%= post.title %></h5>
              <p class="card-text"><%= post.nickname %></p>
              <p class="card-text"><small class="text-body-secondary time_"><%= post.createdAt.toUTCString();%></small></p>
              <button class="btn btn-outline-success" onclick="gotoPost('<%=post.id %>')">자세히 보기</button>            
            </div>
          </div>
        </div>
      </div>
</div>